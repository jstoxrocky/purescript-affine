{
  "name": "affine",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.8.14",
    "pulp": "^16.0.2",
    "purescript": "^0.15.7"
  },
  "scripts": {
    "build": "npx pulp build",
    "test": "npx pulp test",
    "clean": "rm -rf output/",
    "bower-clean": "rm -rf bower_components/ && bower cache clean",
    "bower-install": "npx bower install"
  }
}
